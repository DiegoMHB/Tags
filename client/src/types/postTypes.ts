export type PostType = {
    userId: string,
    need : boolean,
    category : string,
    duration: number,
    title: string,
    description: string,
    location : number[],
    pictures : string[]
}